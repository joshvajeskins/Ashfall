# =============================================
# Ashfall - Environment Variables
# =============================================
# Copy this file to .env.local and fill in the values

# ---------------------------------------------
# CLIENT-SIDE (Public - exposed to browser)
# ---------------------------------------------

# Privy App ID for wallet authentication
# Get from: https://dashboard.privy.io
NEXT_PUBLIC_PRIVY_APP_ID=

# Deployed Ashfall contract address on Movement
# Format: 0x... (64 hex characters)
NEXT_PUBLIC_CONTRACT_ADDRESS=

# API base URL (optional - leave empty for same-origin)
# Only needed if API is hosted separately
NEXT_PUBLIC_API_URL=

# ---------------------------------------------
# SERVER-SIDE (Private - never exposed)
# ---------------------------------------------

# Shinami API Key (Movement Testnet)
# Get from: https://app.shinami.com
# Used for: Gas Station (sponsorship) + Invisible Wallets
SHINAMI_KEY=

# Server Wallet Secret Passphrase
# Used to derive the server's invisible wallet for:
# - Enemy attacks during combat
# - Dungeon floor completion
# - Boss defeat processing
# - Player death handling
# IMPORTANT: Use a strong, unique passphrase in production!
SHINAMI_SERVER_WALLET_SECRET=

# =============================================
# SETUP STEPS:
# =============================================
# 1. Create Privy app at dashboard.privy.io
#    - Enable "Email" login method
#    - Add Movement testnet to supported chains
#
# 2. Deploy contracts:
#    cd contracts && aptos move publish
#
# 3. Get Shinami key at app.shinami.com
#    - Create Movement Testnet project
#    - Enable Gas Station + Wallet Services
#    - Fund the Gas Station with testnet MOVE
#
# 4. Register server wallet as authorized:
#    After first run, check /api/shinami/status for
#    the server wallet address, then run:
#    aptos move run --function-id <CONTRACT>::dungeon::add_server --args address:<SERVER_WALLET>
#    aptos move run --function-id <CONTRACT>::combat::add_server --args address:<SERVER_WALLET>
# =============================================
